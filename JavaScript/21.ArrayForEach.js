let arr = [1,2,3,4,5];
let ans = 0;

// using normal function


// we can get three values by using a forEach method 
// 1. element , 2. index , 3. entire array
arr.forEach(function (element,index,arr){
    
})

arr.forEach((Element) =>{
    ans += Element;
})

console.log(ans);

// forEach does not return anything it only returns undefined